# docs/

Place architecture diagrams and dashboard screenshots here.

Suggested files to add as you complete each phase:

- `architecture.png`   — end-to-end pipeline diagram (export from draw.io / Lucidchart)
- `dbt_lineage.png`    — screenshot of the dbt docs lineage graph
- `dashboard.png`      — screenshot of the Streamlit dashboard
